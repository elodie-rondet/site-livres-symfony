<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Livres{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="{{ asset('assets/main.css') }}" rel="stylesheet">
        <link href="{{ asset('assets/connexion.css') }}" rel="stylesheet">
        <link href="{{ asset('assets/form.css') }}" rel="stylesheet">
        <link rel="icon" href="/favicon.ico">
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <meta name="description" content="A compléter">
        <link rel="apple-touch-icon" href="">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <script>
    $(document).ready(function() {
        $('.js-datepicker').datepicker({
            format: 'yyyy-mm-dd'
        });
    });
</script>
    <header class="Header_Header__wwyjE">
<div class="container"><img class="logo" src="{{ asset('images/logo.png') }}" alt="logo">
<ul><li><a aria-current="page" class="Header_activeLink__-W+SD" href="{{ path('app_home') }}">Accueil</a>
</li>
<li><a href="{{ path('ajoutLivre') }}">Ajouter un livre</a></li>
    {% if app.user %}
        <li><a href="{{ path('app_logout') }}">Logout</a></li>
    {% else %}
        <li><a href="{{ path('app_login') }}">Se connecter</a></li></ul>
    {% endif %}

</div>
</header>
    <body>
        {% block body %}
        {% endblock %}
    </body>
</html>
